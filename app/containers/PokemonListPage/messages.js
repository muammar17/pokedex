/*
 * PokemonListPage Messages
 *
 * This contains all the text for the PokemonListPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PokemonListPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PokemonListPage container!',
  },
});
